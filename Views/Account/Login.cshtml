@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
    ViewData["Title"] = "Login";
}

<div class="d-flex justify-content-center align-items-center min-vh-100">
    <div class="card shadow p-4" style="width: 380px; border-radius: 10px;">
        <div class="card-body">
            <h3 class="text-center fw-bold mb-3">Login</h3>

            <form method="post">
                <!-- Email Field -->
                <div class="mb-3">
                    <label class="fw-semibold">Email</label>
                    <div class="input-group">
                        <span class="input-group-text"><i class="bi bi-envelope"></i></span>
                        <input type="email" class="form-control" name="email" required placeholder="Enter your email" />
                    </div>
                </div>

                <!-- Password Field -->
                <div class="mb-3">
                    <label class="fw-semibold">Password</label>
                    <div class="input-group">
                        <span class="input-group-text"><i class="bi bi-lock"></i></span>
                        <input type="password" class="form-control" name="password" required placeholder="Enter your password" />
                    </div>
                </div>

                <!-- Error Message -->
                @if (ViewData["Error"] != null)
                {
                    <div class="alert alert-danger py-2 text-center">
                        @ViewData["Error"]
                    </div>
                }

                <!-- Forgot Password -->
                <div class="text-end mb-3">
                    <a href="/Account/ForgotPassword" class="text-decoration-none">Forgot Password?</a>
                </div>

                <!-- Submit Button -->
                <button type="submit" class="btn btn-primary w-100 fw-bold" style="border-radius: 5px;">Login</button>
            </form>
        </div>
    </div>
</div>

<!-- Bootstrap Icons (Nếu chưa có trong project) -->
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css">
